# Known-Working Baseline

Baseline date: 2026-09-19

The four files in `app/` are copied from the working version explicitly selected by the project owner as the repository baseline.

This baseline must remain reproducible from `main`. Experimental Player Settings changes belong on a feature branch.
