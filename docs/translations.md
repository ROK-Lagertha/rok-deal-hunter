# Translations

UI translations are data-driven through the Google Sheet where practical.

## Market default language
Each market has one primary/default UI language. For multilingual countries, the default follows the administratively relevant official language that is most widely used nationwide.

Users may still manually select another globally available UI language.

## Fallback
Missing native translations may fall back to English. English fallback text must not be described as completed native localization.
